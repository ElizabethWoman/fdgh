var arr = [3, 9, 23, 64, 2, 8, 28, 93];
console.log(arr[arr.length -1] - arr[0]);
arr.push(101);
console.log(arr[arr.length - 1] - arr[0]);

var names = ['Sam', 'Tommy', 'Tim', 'Sally', 'Buck', 'Bob'];
var length = names.map(function(elements){
return elements.length;
});
console.log(length);
console.log(arr[arr.length -1]);
console.log(arr[0]);

function Seven(word, number){
return word, number;
}
Seven("Hello", "Hello");





function Eight(firstName, lastName){
    console.log(firstName + ' ' + lastName);
}
    Eight( 'Elizabeth', "Pollard");

    function willBuyDrink(isHotOutside, moneyInPocket)
    
